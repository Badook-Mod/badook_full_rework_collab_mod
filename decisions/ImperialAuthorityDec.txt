country_decisions = {

	IA_decision_north_german = { 
	major = no
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			is_emperor = yes
			has_global_flag = IA_land_decision
			NOT = { has_global_flag = IA_north_german }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = north_german_region 
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			north_german_region = { type = all is_core = ROOT owned_by = ROOT }
		}
		effect = {
			add_imperial_influence = 25
			set_global_flag = IA_north_german
		}
	}
	IA_decision_south_german = { 
	major = no
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			is_emperor = yes
			has_global_flag = IA_land_decision
			NOT = { has_global_flag = IA_south_german }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = south_german_region 
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			south_german_region = { type = all is_core = ROOT owned_by = ROOT }
		}
		effect = {
			add_imperial_influence = 25
			set_global_flag = IA_south_german
		}
	}
	
	IA_decision_italy = { 
	major = no
		potential = {
			normal_or_historical_nations = yes
			was_never_end_game_tag_trigger = yes
			is_emperor = yes
			has_global_flag = IA_land_decision
			NOT = { has_global_flag = IA_italy_region }
			is_colonial_nation = no
		}
		provinces_to_highlight = {
			OR = {
				region = italy_region
			}
			OR = {
				NOT = { owned_by = ROOT }
				NOT = { is_core = ROOT }
			}
		}
		allow = {
			is_free_or_tributary_trigger = yes
			is_at_war = no
			south_german_region = { type = all is_core = ROOT owned_by = ROOT }
		}
		effect = {
			add_imperial_influence = 50
			set_global_flag = IA_italy_region
		}
	}